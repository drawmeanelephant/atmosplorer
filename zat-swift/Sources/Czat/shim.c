// Ensures zat.h compiles cleanly as C and gives SPM a source file to build
// for the Czat target (the implementation ships in libzat_c.a, linked via
// the ZatC binary target).
#include "include/zat.h"
